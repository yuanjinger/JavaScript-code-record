JSONP：JSON with padding
	1.script标签
	2.用script标签加载资源是不存在跨域问题的，
	3.script标签加载资源时不时根据后缀名，而是根据资源的内容，只要符合JavaScript数据格式就可以
	4.在资源加载进来之前定义好一个函数，这个函数接收一个参数（数据），函数里面利用这个参数。
	然后需要的时候通过script标签加载对应远程文件资源，当远程的文件资源被加载进来的时候，就会去执行我们前面定义好的函数，并且把数据当作这个函数的参数传入。